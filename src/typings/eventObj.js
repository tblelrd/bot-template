const { Client } = require('discord.js');
module.exports = '';

/**
 * @typedef {object} EventObj
 * @prop {(callback: CallbackObj) => any} callback
 */

/**
 * @typedef {object} CallbackObj
 * @prop {Client} bot
 * @prop {string} prefix
 * @prop {import('./globalVars').GlobalVarObj} globalVars
 */